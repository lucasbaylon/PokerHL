import { OpponentLevelPipe } from './opponent-level.pipe';

describe('OpponentLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new OpponentLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
